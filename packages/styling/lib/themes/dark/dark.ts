import { animation, layout, typography } from '@/core'

// Types
import { ThemeInterface } from '@/types'

// Custom
import { color } from './color'

export const dark: ThemeInterface = {
    animation,
    color,
    layout,
    typography,
}
